// import NotifyWhenUpdatedButton from '../components/lessons/NotifyWhenUpdatedButton';
// import CompleteButton from '../components/lessons/CompleteButton';
// import WatchLaterButton from '. ./components/lessons/watchLaterButton';
export default {
    components: {
        // NotifyWhenUpdatedButton,
        // CompleteButton,
        // WatchLaterButton
    },
/*    mounted() {
        let originalOffsetTop = this.$refs['series-banner-meta'].offsetTop;
        window.addEventListener('scroll', ()=>{
           // console.log('you are scrolling');
            if (window.scrollY >= originalOffsetTop) {
                this.$refs['series-banner-meta'].classList.add(
                    'is-fixed-to-top'
                    //  = 'fixed', 'full', 'pin-t', 'z-10'
                );
            } else {
                this.$refs['series-banner-meta'].classList.remove(
                    'is-fixed-to-top'
                );
            }
        });
    }*/
};
